{{- if .Values.hub.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "lunar.fullname" . }}-badges
  labels:
    {{- include "lunar.labels" . | nindent 4 }}
  {{- with .Values.hub.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.badges.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.badges.ingress.tls }}
  tls:
    {{- range .Values.badges.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.badges.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "lunar.fullname" $ }}-badges
                port:
                  number: {{ $.Values.badges.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
