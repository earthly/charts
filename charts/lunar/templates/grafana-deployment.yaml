{{- if eq .Values.grafana.mode "chart" -}}
{{- $grafanaURL := include "lunar.grafanaURL" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "lunar.fullname" . }}-grafana
  labels:
    {{- include "lunar.labels" . | nindent 4 }}
  {{- with .Values.grafana.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: 1
  {{- if .Values.grafana.persistence.enabled }}
  # A persistent /var/lib/grafana is a single-writer (RWO) PVC. Under the default
  # RollingUpdate the replacement pod starts before the old one releases the
  # volume, so they deadlock on a Multi-Attach error and the rollout hangs.
  # Recreate terminates the old pod first — brief Grafana downtime on upgrade.
  strategy:
    type: Recreate
  {{- end }}
  selector:
    matchLabels:
      {{- include "lunar.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: grafana
  template:
    metadata:
      {{- with .Values.grafana.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "lunar.labels" . | nindent 8 }}
        app.kubernetes.io/component: grafana
        {{- with .Values.grafana.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "lunar.serviceAccountName" . }}
      {{- if or .Values.grafana.persistence.enabled .Values.grafana.podSecurityContext }}
      securityContext:
        {{- if and .Values.grafana.persistence.enabled (not (hasKey .Values.grafana.podSecurityContext "fsGroup")) }}
        # Make the persistent volume group-writable by Grafana (uid/gid 472).
        fsGroup: {{ .Values.grafana.persistence.fsGroup }}
        {{- end }}
        {{- with .Values.grafana.podSecurityContext }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.grafana.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.grafana.image.repository }}:{{ .Values.grafana.image.tag }}"
          imagePullPolicy: {{ .Values.grafana.image.pullPolicy }}
          {{- with .Values.grafana }}
          env:
            {{- with $grafanaURL }}
            - name: GF_SERVER_ROOT_URL
              value: {{ . | quote }}
            {{- end }}
            - name: GF_SECURITY_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "lunar.grafanaAuthSecretName" $ }}
                  key: {{ .auth.userKey }}
            - name: GF_SECURITY_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "lunar.grafanaAuthSecretName" $ }}
                  key: {{ .auth.passwordKey }}
            # Stock upstream Grafana: plugins, datasources and dashboards are
            # installed over the API by the lunar-dashboards provisioning tool (the
            # provisioning hook Job + reconverge sidecar), not baked into the image,
            # so the only server-level knob we set is sign-up. Override via extraEnv.
            - name: GF_USERS_ALLOW_SIGN_UP
              value: "false"
            {{- with .extraEnv }}
              {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          {{- with .Values.grafana.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if or .Values.grafana.persistence.enabled .Values.grafana.volumeMounts }}
          volumeMounts:
            {{- if .Values.grafana.persistence.enabled }}
            - name: grafana-data
              mountPath: /var/lib/grafana
            {{- end }}
            {{- with .Values.grafana.volumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
        - name: kiosk
          image: nginx:1-alpine
          {{- with .Values.grafana.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: kiosk
              containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: kiosk-config
              mountPath: /etc/nginx/conf.d
        # Reconverge sidecar — present in mode=chart (the only mode with a bundled
        # pod); chart mode always provisions, so it has no separate toggle.
        - name: provision-reconverge
          # Re-applies plugins/datasources/dashboards on every pod start so the
          # Grafana pod is stateless/self-healing — a recreated pod (or lost
          # volume) converges back without waiting for the next Hub upgrade.
          # deploy.sh resolves the Grafana endpoint + read-only DB connection from
          # the Hub; GRAFANA_URL points at the Grafana container in this pod.
          image: {{ include "lunar.provisioningImage" . | quote }}
          imagePullPolicy: {{ .Values.grafana.provisioning.image.pullPolicy }}
          {{- with .Values.grafana.provisioning.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              {{- include "lunar.waitForGrafana" . | nindent 14 }}
              {{- include "lunar.waitForHub" . | nindent 14 }}
              /deploy.sh || echo "dashboards reconverge failed" >&2
              # Idle so the Deployment treats this as a long-lived sidecar; it
              # re-applies on the next pod (re)start.
              tail -f /dev/null
          env:
            - name: GRAFANA_URL
              value: "http://localhost:3000"
            {{- include "lunar.grafanaProvisionHubEnv" . | nindent 12 }}
          {{- with .Values.grafana.provisioning.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- range .Values.grafana.volumes }}
      {{- if eq .name "kiosk-config" }}
      {{- fail "Values.grafana.volumes contains reserved name 'kiosk-config'" }}
      {{- end }}
      {{- if and $.Values.grafana.persistence.enabled (eq .name "grafana-data") }}
      {{- fail "Values.grafana.volumes contains reserved name 'grafana-data' (used by grafana.persistence)" }}
      {{- end }}
      {{- end }}
      volumes:
        - name: kiosk-config
          configMap:
            name: {{ include "lunar.fullname" . }}-grafana-kiosk
        {{- if .Values.grafana.persistence.enabled }}
        - name: grafana-data
          persistentVolumeClaim:
            claimName: {{ .Values.grafana.persistence.existingClaim | default (printf "%s-grafana-data" (include "lunar.fullname" .)) }}
        {{- end }}
        {{- with .Values.grafana.volumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.grafana.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.grafana.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.grafana.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}